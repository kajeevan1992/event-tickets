# LocalVibe Event v65 — Organiser Dashboard Analytics

- Added `/admin/dashboard`.
- Added `/api/admin/stats` and `/api/admin/events/:id/stats`.
- Added event revenue, ticket sales, check-in, pending-order and capacity metrics.
- Stripe/payment lifecycle unchanged.
